// Vendors
import { describe, it, expect } from "vitest";
// Pages
import PascalCasePage from "../kebab-case.page";

describe("space case page", () => {
  it("should", () => {
    expect(false).toBe(true);
  });
});
