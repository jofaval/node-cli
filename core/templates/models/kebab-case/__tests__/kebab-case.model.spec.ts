// Vendors
import { describe, it, expect } from "vitest";
// Models
import { PascalCase } from "../kebab-case.model";

describe("space case model", () => {
  it("should", () => {
    expect(false).toBe(true);
  });
});
