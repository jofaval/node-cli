// Vendors
import { describe, it, expect } from "vitest";
// Helpers
import {} from "../kebab-case.helpers";

describe("space case helpers", () => {
  it("should", () => {
    expect(false).toBe(true);
  });
});
