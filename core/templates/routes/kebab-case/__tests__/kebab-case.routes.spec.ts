// Vendors
import { describe, it, expect } from "vitest";
// Routes
import { camelCaseRoute } from "../kebab-case.route";

describe("space case routes", () => {
  it("should", () => {
    expect(false).toBe(true);
  });
});
