// Vendors
import { describe, it, expect } from "vitest";
// Views
import PascalCasePage from "../kebab-case.view";

describe("space case view", () => {
  it("should", () => {
    expect(false).toBe(true);
  });
});
