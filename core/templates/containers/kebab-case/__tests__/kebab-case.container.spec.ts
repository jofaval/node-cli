// Vendors
import { describe, it, expect } from "vitest";
// Containers
import PascalCaseContainer from "../kebab-case.container";

describe("space case container", () => {
  it("should", () => {
    expect(false).toBe(true);
  });
});
